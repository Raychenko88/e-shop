spring:
  datasource:
    url: jdbc:h2:~/e-shop-rs
    username: "admin"
    password: "test"
    hikari:
      connectionTimeout: 20000
      maximumPoolSize: 5
      driver-class-name: "org.h2.Driver"
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: "org.hibernate.dialect.H2Dialect"
#    profiles:
#      active: rest